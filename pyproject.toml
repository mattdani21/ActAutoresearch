[project]
name = "actautoresearch"
version = "0.1.0"
description = "Autonomous research loop over RA charge structures, measured under the ASISA Standard on Effective Annual Cost"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "scipy>=1.11",
]
